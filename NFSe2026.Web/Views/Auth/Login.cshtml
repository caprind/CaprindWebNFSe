@model NFSe2026.Web.Models.LoginViewModel
@{
    ViewData["Title"] = "Login";
    Layout = null;
}

<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - CAPRINDWEB</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            font-weight: 400;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            overflow: hidden;
            background-color: #020617;
        }

        /* Fundo industrial usando imagem + paleta do logotipo (azuis profundos) */
        body::before {
            content: '';
            position: absolute;
            inset: 0;
            background-image:
                linear-gradient(120deg, rgba(2, 6, 23, 0.55), rgba(15, 23, 42, 0.78)),
                url('/img/login-bg-caprindweb.png');
            background-size: cover;
            background-repeat: no-repeat;
            z-index: 0;
            animation: subtleZoom 28s ease-in-out infinite;
        }

        /* Overlay com grid / “HUD” tecnológico em tons de azul */
        body::after {
            content: '';
            position: absolute;
            inset: 0;
            background-image:
                linear-gradient(rgba(148, 163, 184, 0.06) 1px, transparent 1px),
                linear-gradient(90deg, rgba(148, 163, 184, 0.06) 1px, transparent 1px),
                radial-gradient(circle at 20% 30%, rgba(96, 165, 250, 0.18) 0%, transparent 55%),
                radial-gradient(circle at 80% 75%, rgba(56, 189, 248, 0.15) 0%, transparent 55%);
            background-size: 48px 48px, 48px 48px, 380px 380px, 520px 520px;
            mix-blend-mode: soft-light;
            pointer-events: none;
            z-index: 0;
            animation: float 26s ease-in-out infinite;
        }

        @@keyframes subtleZoom {
            0%, 100% {
                transform: scale(1);
                opacity: 1;
            }
            50% {
                transform: scale(1.02);
                opacity: 0.97;
            }
        }

        @@keyframes float {
            0%, 100% { transform: translate(0, 0); }
            33% { transform: translate(22px, -18px); }
            66% { transform: translate(-18px, 14px); }
        }

        .login-container {
            position: relative;
            z-index: 1;
            width: 100%;
            max-width: 660px;
            padding: 14px;
            margin-top: 120px; /* desce o card para não cobrir o logo na imagem em telas maiores */
        }

        .login-card {
            background: rgba(15, 23, 42, 0.35); /* card mais claro/transparente */
            backdrop-filter: blur(20px) saturate(170%);
            -webkit-backdrop-filter: blur(20px) saturate(170%);
            border-radius: 32px;
            box-shadow: 
                0 16px 40px rgba(15, 23, 42, 0.55),
                0 0 0 1px rgba(148, 163, 184, 0.35) inset;
            padding: 1.9rem 2.2rem;
            border: 1px solid rgba(56, 189, 248, 0.55); /* borda em azul-ciano mais corporativo */
            animation: slideUp 0.6s cubic-bezier(0.16, 1, 0.3, 1);
        }

        @@keyframes slideUp {
            from {
                opacity: 0;
                transform: translateY(40px) scale(0.95);
            }
            to {
                opacity: 1;
                transform: translateY(0) scale(1);
            }
        }

        .login-header {
            text-align: center;
            margin-bottom: 1.75rem;
        }

        .login-header img {
            max-width: 120px;
            height: auto;
            margin-bottom: 0.75rem;
        }

        .login-header h1 {
            font-size: 1.6rem;
            font-weight: 700;
            letter-spacing: 0.05em;
            text-transform: uppercase;
            color: #e5e7eb;
        }

        .login-header .tagline {
            color: #9ca3af;
            font-size: 0.85rem;
            font-weight: 500;
            letter-spacing: 0.12em;
            text-transform: uppercase;
            margin-bottom: 0.35rem;
        }

        .login-header .helper {
            color: #cbd5f5;
            font-size: 0.9rem;
            font-weight: 400;
        }

        .form-group {
            margin-bottom: 1.5rem;
            position: relative;
        }

        .form-group-with-icon {
            position: relative;
        }

        .form-control-icon {
            position: absolute;
            left: 1rem;
            top: 50%;
            transform: translateY(-50%);
            font-size: 1.2rem;
            pointer-events: none;
            z-index: 2;
        }

        .form-control {
            padding: 0.875rem 1rem 0.875rem 3rem;
            border: 2px solid #e2e8f0;
            border-radius: 14px;
            font-size: 1rem;
            font-weight: 400;
            font-family: 'Inter', sans-serif;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            background-color: rgba(255, 255, 255, 0.8);
            backdrop-filter: blur(10px);
            color: #1e293b;
            text-align: left;
            letter-spacing: 0.01em;
        }

        .form-control:focus {
            border-color: #2563eb;
            background-color: #fff;
            box-shadow: 0 0 0 4px rgba(37, 99, 235, 0.12), 0 4px 12px rgba(37, 99, 235, 0.15);
            outline: none;
            transform: translateY(-2px);
        }

        .form-control::placeholder {
            color: #94a3b8;
            font-weight: 400;
        }

        .btn-login {
            width: 100%;
            padding: 1rem;
            font-size: 1.1rem;
            font-weight: 600;
            font-family: 'Inter', sans-serif;
            letter-spacing: 0.3px;
            border-radius: 14px;
            background: linear-gradient(135deg, #1e40af 0%, #2563eb 100%);
            border: none;
            color: white;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            margin-top: 0.5rem;
            position: relative;
            overflow: hidden;
            box-shadow: 0 4px 14px rgba(37, 99, 235, 0.4);
        }

        .btn-login::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.2);
            transform: translate(-50%, -50%);
            transition: width 0.6s, height 0.6s;
        }

        .btn-login:hover::before {
            width: 300px;
            height: 300px;
        }

        .btn-login:hover {
            transform: translateY(-3px);
            box-shadow: 0 12px 32px rgba(37, 99, 235, 0.5);
            color: white;
            background: linear-gradient(135deg, #1e3a8a 0%, #1e40af 100%);
        }

        .btn-login:active {
            transform: translateY(0);
        }

        .btn-login span {
            position: relative;
            z-index: 1;
        }

        .forgot-password {
            text-align: right;
            margin-top: 0.25rem;
            margin-bottom: 1.25rem;
        }

        .forgot-password a {
            color: #2563eb;
            text-decoration: none;
            font-size: 0.95rem;
            font-weight: 600;
            font-family: 'Inter', sans-serif;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 0.25rem;
        }

        .forgot-password a:hover {
            color: #1e40af;
            text-decoration: none;
            transform: translateX(-2px);
        }

        .register-link {
            text-align: center;
            margin-top: 1.75rem;
            padding-top: 1.5rem;
            border-top: 1px solid #e2e8f0;
        }

        .register-link p {
            color: #64748b;
            margin-bottom: 0;
            font-size: 0.95rem;
            font-weight: 400;
            font-family: 'Inter', sans-serif;
        }

        .register-link a {
            color: #2563eb;
            text-decoration: none;
            font-weight: 700;
            font-family: 'Inter', sans-serif;
            transition: all 0.3s ease;
            letter-spacing: 0.2px;
        }

        .register-link a:hover {
            color: #1e40af;
            text-decoration: underline;
        }

        .alert {
            border-radius: 12px;
            border: none;
            margin-bottom: 1.25rem;
            padding: 0.9rem 1.1rem;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
            font-family: 'Inter', sans-serif;
            font-weight: 500;
        }

        .field-validation-error {
            color: #dc3545;
            font-size: 0.85rem;
            margin-top: 0.4rem;
            display: block;
            font-weight: 500;
            font-family: 'Inter', sans-serif;
        }

        .login-footer {
            position: fixed;
            left: 0;
            right: 0;
            bottom: 12px;
            text-align: center;
            font-size: 0.8rem;
            color: #9ca3af;
            z-index: 1;
        }

        @@media (max-width: 576px) {
            body {
                align-items: flex-start;
            }

            .login-container {
                max-width: 420px;
                margin-top: 40px; /* sobe o card em telas pequenas para caber melhor */
            }

            .login-card {
                padding: 1.5rem 1.4rem;
                border-radius: 24px;
            }

            .login-header h1 {
                font-size: 1.4rem;
            }
        }
    </style>
</head>
<body>
    <div class="login-container">
        <div class="login-card">
            <div class="login-header">
                <img src="~/img/logo-caprindweb.png" alt="CAPRINDWEB" />
                <h1>CAPRINDWEB</h1>
                <p class="tagline">Gestão Industrial 5.0</p>
                <p class="helper">Completo • Compacto • Específico para a sua empresa.</p>
            </div>

            @if (!ViewData.ModelState.IsValid)
            {
                <div class="alert alert-danger" role="alert">
                    <strong>Ops, algo não deu certo.</strong>
                    <ul class="mb-0 mt-2">
                        @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                        {
                            <li>@error.ErrorMessage</li>
                        }
                    </ul>
                </div>
            }

            <form asp-action="Login" method="post">
                <div class="form-group form-group-with-icon">
                    <span class="form-control-icon">📧</span>
                    <input asp-for="Email" class="form-control" placeholder="seunome@empresa.com.br" autocomplete="email" />
                    <span asp-validation-for="Email" class="field-validation-error"></span>
                </div>

                <div class="form-group form-group-with-icon">
                    <span class="form-control-icon">🔒</span>
                    <input asp-for="Senha" class="form-control" type="password" placeholder="Sua senha de acesso" autocomplete="current-password" />
                    <span asp-validation-for="Senha" class="field-validation-error"></span>
                </div>

                <div class="forgot-password">
                    <a asp-action="EsqueciSenha">
                        <span>🔑</span>
                        Esqueci minha senha
                    </a>
                </div>

                <button type="submit" class="btn btn-login">
                    <span>Entrar →</span>
                </button>
            </form>

            <div class="register-link">
                <p>Não tem uma conta? <a asp-action="Cadastro">Cadastre-se aqui</a></p>
            </div>
        </div>
    </div>

    <footer class="login-footer">
        © 1999–@DateTime.Now.Year Caprind Sistemas®. Todos os direitos reservados.
    </footer>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/lib/jquery-validation/dist/jquery.validate.min.js"></script>
    <script src="~/lib/jquery-validation-unobtrusive/jquery.validate.unobtrusive.min.js"></script>
</body>
</html>
